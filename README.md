Sources for the plugin `plugin.proximitySensor`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.proximitySensor" = {
            publisherId = "tech.scotth",
        },
    },
}
```
